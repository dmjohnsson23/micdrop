[tool.poetry]
name = "micdrop"
version = "0.1.0"
description = "Tool to aid in migrating, mappibg, importing, converting, and transforming data."
authors = ["Dominick Johnson <dominick.johnson@tylertech.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
